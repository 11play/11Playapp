<?xml version="1.0" encoding="utf-8"?>

<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/rootContainer"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/elevenplay_background">

    <!-- =====================================================
         11PLAY MAIN WEBVIEW

         Pull-to-refresh has been completely removed.
         Normal WebView scrolling remains unchanged.
    ====================================================== -->

    <WebView
        android:id="@+id/webView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/elevenplay_background"
        android:overScrollMode="never" />


    <!-- =====================================================
         PAGE LOADING PROGRESS
    ====================================================== -->

    <ProgressBar
        android:id="@+id/pageProgress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="3dp"
        android:layout_gravity="top"
        android:max="100"
        android:progress="0"
        android:visibility="gone"
        android:indeterminate="false"
        android:progressTint="@color/elevenplay_blue"
        android:progressBackgroundTint="@android:color/transparent" />


    <!-- =====================================================
         FLOATING MANUAL REFRESH BUTTON

         Tap:
             Reload current WebView page.

         Drag:
             Move horizontally left/right.

         Release:
             MainActivity snaps it to the nearest edge.

         The button stays above the WebView and does not
         move when the website itself is scrolled.
    ====================================================== -->

    <ImageButton
        android:id="@+id/floatingRefreshButton"
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:layout_gravity="end|center_vertical"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="12dp"
        android:background="@drawable/bg_floating_refresh"
        android:src="@drawable/ic_refresh"
        android:padding="12dp"
        android:scaleType="centerInside"
        android:contentDescription="Refresh"
        android:clickable="true"
        android:focusable="true"
        android:elevation="8dp"
        android:alpha="0.94" />

</FrameLayout>
